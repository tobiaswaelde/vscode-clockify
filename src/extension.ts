import * as vscode from 'vscode';
import { Clockify } from './sdk/workspaces';

export function activate(context: vscode.ExtensionContext) {
	console.log('[clockify-tracker] Activating extension...');
}

export function deactivate() {}
